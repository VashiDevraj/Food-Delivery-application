<?xml version="1.0" encoding="utf-8"?>
<!--
    item_coupon.xml — polished coupon card
    IDs: tvCouponCode, tvCouponDesc, tvCouponConditions, tvCouponDiscount, btnApplyCoupon
    CHANGE: tvCouponConditions visibility is now handled in the adapter (gone/visible).
            Discount badge supports multi-line text for "FREE\nDELIVERY".
-->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="14dp"
    android:layout_marginEnd="14dp"
    android:layout_marginTop="6dp"
    android:layout_marginBottom="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- ── Top band: icon + code + badge ────────────────────────────── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="14dp"
            android:paddingBottom="12dp"
            android:background="#FFF5F5">

            <!-- Ticket emoji in a coloured circle -->
            <TextView
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:text="🎟️"
                android:textSize="20sp"
                android:gravity="center"
                android:background="@drawable/bg_circle_red_light"
                android:layout_marginEnd="12dp" />

            <!-- Code + description -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvCouponCode"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="#E23744"
                    android:letterSpacing="0.06" />

                <TextView
                    android:id="@+id/tvCouponDesc"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:textSize="13sp"
                    android:textColor="#444444" />
            </LinearLayout>

            <!-- Discount badge — supports 2-line text (FREE\nDELIVERY) -->
            <LinearLayout
                android:layout_width="58dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:background="@drawable/bg_total_red">

                <TextView
                    android:id="@+id/tvCouponDiscount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:textColor="#FFFFFF"
                    android:gravity="center"
                    android:lines="2"
                    android:maxLines="2" />
            </LinearLayout>
        </LinearLayout>

        <!-- ── Divider ──────────────────────────────────────────────────── -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#FFE5E5" />

        <!-- ── Conditions + Apply button ────────────────────────────────── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="16dp"
            android:paddingEnd="12dp"
            android:paddingTop="10dp"
            android:paddingBottom="10dp">

            <!-- Condition text — adapter sets visibility -->
            <TextView
                android:id="@+id/tvCouponConditions"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:textSize="11.5sp"
                android:textColor="#9E9E9E"
                android:visibility="gone" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnApplyCoupon"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:text="Apply"
                android:textColor="#E23744"
                android:textSize="13sp"
                android:textStyle="bold"
                app:backgroundTint="#FFF5F5"
                app:cornerRadius="18dp"
                app:strokeColor="#E23744"
                app:strokeWidth="1dp" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>